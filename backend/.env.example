# Copie para backend/.env ou para .env na raiz do projeto (não commite .env).
PORT=8080

# Knowledge Assistant (KA) – obrigatório para /api/chat
KA_ENDPOINT_URL=
KA_TOKEN=

# Forecast (/api/forecast) – Databricks SQL
DATABRICKS_HOST=
DATABRICKS_WAREHOUSE_ID=
